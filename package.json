{
  "name": "sophiazempel.github.io",
  "private": true,
  "devDependencies": {
    "gh-pages": "^0.12.0",
    "live-server": "^1.2.0"
  },
  "scripts": {
    "publish": "gh-pages -d src -b master",
    "start": "live-server src"
  }
}
